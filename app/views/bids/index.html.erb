<h1>Listing Bids</h1>
  <% @events.each do |event| %>
    <% if event.active == true %>
      <%= link_to event.id, event_path(event) %>
      <%= event.bids.each do |bid| %>
        <%= bid.user_choice %>
        <% end %>
    <% else %>
      <%= puts "" %>
    <% end %>
  <% end %>

<p><%= "hi #{@events.count}" %></p>
<table>
  <thead>
    <tr>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
  </tbody>
</table>


<br>