<%= form_for :bid, @event.bids.new do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :user_choice %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
