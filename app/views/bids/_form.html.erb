<%= form_for :bid, :url event_bids_path(@event) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :user_choice %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>

<% end %>
